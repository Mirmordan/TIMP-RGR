# Зависимости
node_modules/

# Артефакты сборки
dist/
build/
*.tsbuildinfo

# Данные рантайма: видеочанки и записи (гигабайты, не в VCS)
data/
ffmpeg-manager/recordings/
media-manager/recordings/

# Логи и артефакты инструментов
*.log
.playwright-mcp/

# Системное
.DS_Store

# Инструменты ассистента разработки (не в VCS; конфиги в ~/.config/opencode/)
AGENTS.md
CLAUDE.md
.opencode/
.claude/

# ВАЖНО: .env и конфиги (docker-compose.yml, mediamtx.yml, nginx.conf) намеренно
# НЕ игнорируются — учебный проект, должны попадать в репозиторий.
# Бэкапы и локальные артефакты
backups/

# Серверные локальные переопределения compose (не в репе)
docker-compose.override.yml
