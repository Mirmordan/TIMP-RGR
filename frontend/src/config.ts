/** Конфигурация фронтенда. Все относительные пути — проксируются через Vite. */
export const config = {
  /** Префикс API (совпадает с бэкендом). */
  apiPrefix: '/api/v1',

  /** Интервал обновления данных (мс). */
  pollInterval: 5000,

  /** Название приложения. */
  appName: 'ТИМП — Система записи',
} as const;
